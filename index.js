const { readWorkBook, translateWorkBook, readAndTranslate } = require('./read')
const Plugins = require('./plugin')

module.exports = {
  readWorkBook,
  translateWorkBook,
  readAndTranslate,
  Plugins
}
